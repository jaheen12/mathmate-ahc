import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jaheen12.mathmateahc',
  appName: 'MathMate AHC',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;